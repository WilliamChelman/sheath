// Phosphor icons from /regular variant
import { phosphorFileText } from '@ng-icons/phosphor-icons/regular';

/**
 * Registry mapping icon names (as strings) to their actual icon imports.
 * When adding a new icon to a classConfig, import it here and add it to the registry.
 */
export const entityIconRegistry: Record<string, unknown> = {
  phosphorFileText, // Fallback icon
};
